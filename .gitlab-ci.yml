stages:
  - run

variables:
  GIT_STRATEGY: none
  TIMEOUT: 600

run_link_monitor:
  stage: run
  image: python:3.12
  timeout: 10m
  script:
    # ---------------------------------------------------
    # 1. Instalăm AWS CLI & SSH tools
    # ---------------------------------------------------
    - apt-get update -y
    - apt-get install -y awscli openssh-client

